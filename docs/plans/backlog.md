# 数据结构深化与扩充 · 候选池（Backlog）

> Status: active
> Owner: IllegalCreed
> Created: 2026-06-25
> Last reviewed: 2026-06-26
> Related milestone: roadmap.md M4

## 背景

M3 已完成**基础数据结构 8/8**（栈/队列/数组/链表/树/堆/哈希/图）的「可探索讲解」科普巡览。盘点后确认：广度上**基础结构已齐**，但两处有空间——① 多篇当年为聚焦**主动砍掉了常见子类**（见各页 requirements「不做什么」）；② 还有一批**很常用但完全没覆盖**的结构。

约定推进策略（2026-06-25 与 Owner 敲定）：**先把待补项全部记录在本池 → 优先补深度（现有页深化）→ 再补广度（新增结构）→ 一项一项按序走完整 7 步流程**（brainstorming + 原型 → 四文档 → TDD → 全门禁 → 三索引回写 → 两提交 → 双轨部署）。

候选项默认 `candidate`；启动某项时再分配 Change ID（顺延 C-20260625-023…）、建 plan 四文档、把该行移入 `docs/plans/index.md`。

## 推进顺序总览

> 深度（D）优先、广度（B）其次；同类内按下表序号。落点列区分「现有页加节」（无需动菜单/路由/首页）与「新页/姊妹页」（需新增菜单 + 路由 + 首页条目 + 图标）。

| 序     | 类别      | 项                                          | 落点       | 价值 / 一句话                                            | 状态             |
| ------ | --------- | ------------------------------------------- | ---------- | -------------------------------------------------------- | ---------------- |
| ~~D1~~ | 深度·树   | ~~平衡树（红黑/AVL）思想~~                  | 树页加节   | 还 callout 欠的债：BST 会退化成链 → 旋转/平衡保 O(log n) | **✓ 完成 C-023** |
| ~~D2~~ | 深度·哈希 | ~~开放寻址 + 装载因子~~（rehash 留后续）    | 哈希页加节 | 与拉链法对照的第二种解冲突；线性探测成簇 + 装载因子      | **✓ 完成 C-024** |
| ~~D3~~ | 深度·链表 | ~~双向链表（prev 指针）~~                   | 链表页加节 | 很常见；前驱可直接删、双向遍历（循环链表一句带过）       | **✓ 完成 C-025** |
| ~~D4~~ | 深度·队列 | ~~双端队列 deque~~（优先/循环队列正文带过） | 队列页加节 | 两端都能进出；滑动窗口；优先队列≈堆                      | **✓ 完成 C-026** |
| ~~D5~~ | 深度·数组 | ~~动态数组扩容（均摊 O(1)）~~               | 数组页加节 | 翻倍扩容 + 摊还分析这个漂亮论证                          | **✓ 完成 C-027** |
| ~~B1~~ | 广度      | ~~字典树 Trie 前缀树~~                      | 新页       | 自动补全、词典、前缀匹配；树家族延伸、视觉好             | **✓ 完成 C-028** |
| ~~B2~~ | 广度      | ~~并查集 Union-Find~~                       | 新页       | 连通性、Kruskal、岛屿合并；合并+找根+路径压缩，出彩      | **✓ 完成 C-029** |
| ~~B3~~ | 广度      | ~~LRU 缓存（哈希 + 双链表）~~               | 新页       | 经典组合结构、面试常考；呼应前面两结构联动               | **✓ 完成 C-031** |
| B4     | 广度·进阶 | 跳表 Skip List                              | 新页       | Redis 有序集合                                           | candidate        |
| B5     | 广度·进阶 | 线段树 / 树状数组（Fenwick）                | 新页       | 区间查询（竞赛向）                                       | candidate        |
| B6     | 广度·进阶 | B 树 / B+ 树                                | 新页       | 数据库 / 文件系统索引                                    | candidate        |
| B7     | 广度·进阶 | 布隆过滤器 Bloom Filter                     | 新页       | 海量去重 / 存在性（概率型）                              | candidate        |

> 注：若 D1 只讲「平衡的思想」，完整旋转实现可后挪到广度进阶单独成页。

## 深度项细节（现有页对应的「当年所砍」）

各页 requirements 的「不做什么」即本池深度项来源，便于复用当时的论证：

- **树**（C-019）：弃了**平衡树 AVL/红黑**、删除节点、n 叉树、序列化 → D1（+ 删除可顺带）。
- **哈希**（C-021）：弃了**开放寻址**、**rehash 扩容**、真实字符串散列、删除 → D2。
- **链表**（C-018）：弃了**双向链表**、循环链表、跳表 → D3（跳表见 B4）。
- **队列**（C-016）：弃了**循环队列 / 双端队列 / 优先队列** → D4。
- **数组**（C-017）：弃了**动态扩容**、多维、稀疏 → D5。
- **堆**（C-020）：弃了小顶堆（镜像）、heapify 建堆、d 叉堆 → 可选小补（优先级低）。
- **图**（C-022）：弃了有向 / 带权 / Dijkstra / 拓扑 / 最小生成树 / 邻接矩阵互动 → **归「图算法」独立轨道，不在本池**（见下）。

## 不在本池（另列）

- **图算法线**：有向图 / 带权图 / 最短路（Dijkstra·Bellman-Ford）/ 拓扑排序 / 最小生成树（Prim·Kruskal）/ 连通分量——这些更像「算法」而非「结构」，建议单独里程碑/菜单分类，不混进数据结构。
- **通用化重构**：把树/图二维布局抽成可复用插槽组件（工程优化，非内容）。

## 落点决策须知（启动每项前定）

- **现有页加节（D1–D5 多数）**：只改对应 `*.vue` 正文 +（可能）新互动子组件；**不动菜单/路由/首页**。注意正文变长是否拆姊妹页。
- **新页（B1–B7）**：需新增 侧边菜单条目（`Docs/Menu/hooks.ts`）+ 路由（`router/index.ts`）+ 首页网格条目 + 图标（`assets/*.svg`）+ `Article/DataStructure/Xxx.vue`，与 M3 各结构同范式。
- Case ID 命名空间：复用页加节沿用原页命名；新结构用新前缀（如 `TC-TRIE-*` / `TC-UNIONFIND-*`），避让既有。

## 变更历史

- 2026-06-25：创建。M3 数据结构 8/8 收官后盘点深度 + 广度，记录 5 个深度项（D1–D5）+ 7 个广度项（B1–B7）候选，约定「先深度后广度、按序推进」。图算法线另列。
- 2026-06-26：D2「开放寻址」出池完成（C-024，哈希页加节「另一种解冲突：开放寻址」，线性探测 + 装载因子，rehash 扩容留后续）。深度进度 D1✓ D2✓，下一项 D3 链表·双向。
- 2026-06-26：D3「双向链表」出池完成（C-025，链表页加节「双向链表：再加一根 prev 指针」，反向遍历 + 无需找前驱的 O(1) 删除，循环链表正文带过）。深度进度 D1✓ D2✓ D3✓，下一项 D4 队列·双端。
- 2026-06-26：D4「双端队列」出池完成（C-026，队列页加节「双端队列 Deque：两端都能进出」，四向进出 + 栈/队列的共同推广，优先/循环队列正文带过）。深度进度 D1✓ D2✓ D3✓ D4✓，下一项 D5 数组·扩容（**深度收官**）。
- 2026-06-26：D5「动态数组扩容」出池完成（C-027，数组页加节「动态数组：容量满了怎么办——翻倍扩容」，翻倍扩容 + 摊还分析均摊 O(1)）。**深度 D1–D5 全部收官**；下一步 M4 转**广度**，从 B1 字典树 Trie 起（新页：需新增菜单 + 路由 + 首页 + 图标）。
- 2026-06-26：B1「字典树 Trie」出池完成（C-028，**首个广度新结构、首个新页**：固定词集 + 查找三结局 + 前缀自动补全；4 处接线 + 改 2 HOOK 计数 8→9）。数据结构页 8→9。下一项 B2 并查集 Union-Find（新页：合并 + 找根 + 路径压缩）。
- 2026-06-27：B2「并查集 Union-Find」出池完成（C-029，新页：固定 8 元素 + 父指针箭头 + find/union/连通 + 路径压缩；4 处接线 + 改 2 HOOK 计数 9→10）。数据结构页 9→10。下一项 B3 LRU 缓存（哈希 + 双链表组合结构）。
- 2026-06-29：B3「LRU 缓存」出池完成（C-031，新页：哈希表 + 双向链表组合，按最近使用排序 + 满了淘汰最久没用，回扣哈希/双向链表；4 处接线 + 改 2 HOOK 计数 10→11）。数据结构页 10→11。B1–B3 常用广度结构（Trie/并查集/LRU）全部完成；下一档 B4+ 进阶（跳表/线段树·树状数组/B 树/布隆过滤器，竞赛/工程向，优先级低于已完成项）。
