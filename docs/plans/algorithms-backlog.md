# 算法分类扩充 · 候选池（Backlog）

> Status: active
> Owner: IllegalCreed
> Created: 2026-06-29
> Last reviewed: 2026-06-29
> Related milestone: roadmap.md M6

## 背景

到 2026-06-29，站点内容已覆盖**数据结构 15 个**（M3 基础 8 + M4 扩充 7：Trie/并查集/LRU/跳表/线段树/B+树/布隆）+ **排序算法 8 个**。但「算法」侧除排序外几乎空白——还有一大批**算法范式/领域**没有覆盖。

本池负责**规划这条「算法分类扩充」线**（roadmap M6，产品内容线），与别的会话在做的 M5（SEO/GEO 增长线）**并行、互不冲突**。

历史依据：数据结构候选池 [backlog.md](./backlog.md) 第 48/52 行早已约定——「图算法（有向/带权/最短路/拓扑/MST/连通分量）更像『算法』而非『结构』，单独里程碑/菜单分类，不混进数据结构」。本池正式承接这条独立轨道，并把它扩展为完整的算法分类规划。

## 核心策略（2026-06-29 与 Owner 敲定）

1. **先把后续要补的算法大类全部记录在本池**（本文档），分三阶段排好优先级，避免以后重新盘点。
2. **按「算法范式/领域」新增顶层菜单大类**——和现有「数据结构」「经典排序算法」并列；每个大类下挂多个算法页。
3. **每个大类首发**时：①顺带建**该类共用的可视化基础**（图画布 / DP 表格 / 决策树……，类似当年 C-006 先建算法播放器框架）；②新增**顶层菜单分类**接线；③接入第一个代表算法。之后同类每个算法 = 一个新页。
4. 一项一项走完整流程：设计要点 → 四文档 → TDD（L3→L4→L5）→ 全门禁 → 三索引回写 → 两提交 → 双轨部署。
5. 候选项默认 `candidate`；启动某项再分配 Change ID（顺延全局计数；**开工前先 `grep -rn "C-202..." docs/plans/index.md` 确认号没被别的会话占**）、建四文档、行入 `docs/plans/index.md`。

## 三阶段总览

> 阶段一（核心范式，可视化收益 + 教学价值最高）→ 阶段二（成熟范式、受众广、可复用现成框架）→ 阶段三（进阶/工具）。同阶段内按下表序，**先做带 ★ 的「首发项」**（建该大类骨架 + 共用可视化）。

| 阶段   | 大类（新顶层分类） | 首发项 ★                    | 共用可视化基础                               | 价值 / 一句话                               | 状态                    |
| ------ | ------------------ | --------------------------- | -------------------------------------------- | ------------------------------------------- | ----------------------- |
| **一** | 图算法 (G)         | ~~Dijkstra 最短路~~（G1 ✓） | **图画布**（新建自包含，复用 GraphViz 画法） | 承接「图」结构、受众最广、节点/边动画最好看 | **G1 ✓ C-037·大类开张** |
| **一** | 动态规划 (DP)      | 0/1 背包                    | **DP 表格填充**（新范式）                    | 面试/竞赛重灾区、最缺好可视化、差异化最强   | candidate               |
| **一** | 回溯与搜索 (BT)    | N 皇后                      | **决策树 / 棋盘走查**（新范式）              | 递归展开 + 剪枝回退，观赏性最强             | candidate               |
| **二** | 字符串算法 (STR)   | KMP                         | 复用**算法播放器**（双串 + 指针）            | 已有 Trie 打底；next 数组 + 双指针很适配    | candidate               |
| **二** | 查找算法 (SR)      | 二分查找及变体              | 复用**排序柱状条** + 指针                    | 轻量；区间收缩可视化经典                    | candidate               |
| **三** | 数学与数论 (NT)    | 欧几里得 GCD                | 步骤面板 / 数值变化                          | 工具性、受众稍窄                            | candidate               |
| **三** | 算法思想专题 (PA)  | 双指针 + 滑动窗口           | 复用柱状条 / 序列                            | 把零散「技巧」合一类讲思想                  | candidate               |

## 各大类代表算法清单（★ = 建类首发项）

### 图算法 (G) —— 阶段一

> **可视化基础**：扩展现有 `src/components/structures/GraphViz.vue` + `useGraph.ts`（C-022 图数据结构页已实现无向图 + BFS/DFS 基础）为**带权 / 有向图 + 算法走查点亮**（访问中 / 已确定 / 正在松弛的边 / 距离表）。固定图结构（仿数据结构页固定结构的可测做法）。

- **G1 ★ Dijkstra 单源最短路**（非负权）：优先取当前最近点 → 松弛邻边 → 更新距离表。建类首发。**✓ 完成 C-037**（固定 6 点 9 边带权有向图、单步松弛演示；新建自包含 useDijkstra/DijkstraViz，未抽共用画布——留给 G3/G7 等第二个图算法时）。
- **G2 BFS / DFS 遍历（算法视角）**：C-022 已讲基础版——本类可**直接链接复用**或加强（无权最短路 = BFS 层），优先级低、可跳过。
- **G3 Bellman-Ford**：可处理负权 + 判负环（V-1 轮松弛）。
- **G4 Floyd-Warshall**：多源最短路（三重循环 DP 矩阵，呼应 DP 类）。
- **G5 拓扑排序**：Kahn 入度法 / DFS 逆后序（DAG）。
- **G6 最小生成树 Kruskal**：排序边 + **并查集**判环（呼应已有并查集页）。**✓ 完成 C-038**（固定 6 点 9 边无向带权图、单步判环演示、MST 5 条总权 18、跳过 4 条成环）。
- **G7 最小生成树 Prim**：从点集生长、每次取最小横切边。
- **G8 连通分量 / 强连通分量**：并查集 / Tarjan / Kosaraju。

### 动态规划 (DP) —— 阶段一

> **可视化基础**：新建 **DP 表格**组件——网格逐格填值 + 高亮「当前格」与它「依赖的来源格」(转移箭头) + 终态回溯出最优路径。这是全新范式，首发项要单独设计这套（最值得一次性打磨好）。

- **DP1 ★ 0/1 背包**：二维表（物品 × 容量），逐格 max(不选, 选)。建类首发。
- **DP2 最长公共子序列 LCS**：二维表 + 回溯路径还原序列。
- **DP3 最长递增子序列 LIS**：一维 DP + 二分优化对照。
- **DP4 编辑距离 Levenshtein**：二维表，插/删/替三来源取 min。
- **DP5 完全背包 / 多重背包**：与 0/1 对照（循环方向之差）。
- **DP6 区间 DP**：石子合并 / 矩阵链乘（按区间长度递推）。
- **DP7 入门引子**：爬楼梯 / 斐波那契——记忆化递归 vs 递推对照（可作首发备选，更轻）。

### 回溯与搜索 (BT) —— 阶段一

> **可视化基础**：新建**递归决策树**（节点逐步展开 + 剪枝/回退标记）或**棋盘/网格走查**（放置 + 冲突高亮）。

- **BT1 ★ N 皇后**：棋盘逐行放置 + 列/对角冲突剪枝 + 回退。建类首发。
- **BT2 全排列**：决策树展开 + 已用标记。
- **BT3 组合 / 子集生成**：选 / 不选的二叉决策。
- **BT4 数独求解**：格子候选 + 回溯。
- **BT5 迷宫 / 岛屿 DFS**：网格回溯（呼应图遍历）。
- **BT6 括号生成 / 电话号码字母组合**：经典回溯小品。

### 字符串算法 (STR) —— 阶段二

> **可视化基础**：复用现成**算法播放器**（多语言代码 + 行高亮 + 单步 + 变量面板）——主串 / 模式串两排格子 + 双指针 + next 数组。已有 Trie 可呼应。

- **STR1 ★ KMP**：失配函数 next 构建 + 主串模式串双指针不回溯。建类首发。
- **STR2 Rabin-Karp**：滚动哈希匹配。
- **STR3 Manacher**：最长回文子串（中心扩展 + 半径数组）。
- **STR4 Z 函数 / 扩展 KMP**。
- **STR5 AC 自动机**：Trie + fail 指针多模式匹配（呼应已有 Trie）。

### 查找算法 (SR) —— 阶段二

> **可视化基础**：复用排序的柱状条 + 指针（区间 [lo, hi] 收缩高亮）。

- **SR1 ★ 二分查找及变体**：有序数组区间收缩；含找左/右边界、旋转数组。建类首发。
- **SR2 跳跃查找 Jump Search**：分块跨步 + 块内线性。
- **SR3 插值查找**：按值估计位置（均匀分布）。
- **SR4 三分查找**：单峰函数求极值。

### 数学与数论 (NT) —— 阶段三

> **可视化基础**：步骤面板 / 数值逐步变化（多可复用播放器的变量面板）。

- **NT1 ★ 欧几里得 GCD + 扩展欧几里得**：辗转相除可视化。建类首发。
- **NT2 埃氏筛 / 线性筛**：素数筛除标记动画。
- **NT3 快速幂**（含矩阵快速幂）：指数二进制拆分。
- **NT4 质因数分解**。
- **NT5 模运算 / 逆元**。

### 算法思想专题 (PA) —— 阶段三

> **可视化基础**：复用柱状条 / 序列。把零散「技巧」合成一类讲**思想**，单个内容少、合并更合适。

- **PA1 ★ 双指针 + 滑动窗口**：数组上两指针 / 窗口伸缩。建类首发。
- **PA2 前缀和 / 差分**：预处理 + 区间查询（呼应线段树）。
- **PA3 贪心**：活动选择 / 区间调度 / 找零 / 霍夫曼。
- **PA4 分治**：最近点对 / 快速幂思想 / 归并思想回顾。
- **PA5 二分答案**：把「求解」转「判定 + 二分」。

## 落点决策须知（启动每项前定）

- **新增顶层大类**（每个大类首发那次）：
  - `src/views/Docs/Menu/hooks.ts` 的 `useCategoryData` 追加一个分类组（目前返回「数据结构 / 经典排序算法」2 个，将扩成 N 个）。
  - `src/views/Home/Main/hooks.ts` 首页网格追加同名分类组。
  - **改 `TC-HOOK-01-1`**（现断言「返回数据结构与排序两个分类」→ 改为 N 个分类）+ **`TC-HOOK-02-*`**（分类数 / children 数）。注意这两条会随每次「新增大类」变动。
- **该类每个算法页 = 新页 4 处接线**（已跑通 7 次）：路由（`router/index.ts`）+ 菜单 children + 首页条目 + 图标（`assets/*.svg`，1024 viewBox 黑剪影）。
- **共用可视化基础**：图画布（扩展 GraphViz）/ DP 表格（新建）/ 决策树（新建）——首发项内一并建好，后续同类算法复用，不重造。
- **可测范式**：沿用「固定结构 / 固定输入 + 纯逻辑 useXxx（L3）+ XxxViz 同步置态（L4）+ 视图 + e2e（L5）」；高亮同步置态 + CSS 过渡渐显（不用 setTimeout 步进），保证精确计数可断言。
- **Case ID 命名空间**：新前缀避让既有——如 `TC-DIJKSTRA-*` / `TC-KNAPSACK-*` / `TC-NQUEEN-*` / `TC-KMP-*` / `TC-BSEARCH-*` 等。
- **编号**：顺延全局计数 `C-YYYYMMDD-NNN`；**开工前 grep `docs/plans/index.md` 确认下一个号没被别的会话占用**（同一工作树有 M5 会话并行）。

## 不在本池（太重 / 可视化收益低，按需另议）

- 网络流（最大流 / 最小割）、二分图匹配（匈牙利 / KM）。
- 字符串后缀数组 / 后缀自动机、计算几何（凸包 / 扫描线）。
- 高级数据结构（平衡树完整实现 / 可持久化 / LCT / 主席树）——若做归数据结构线另议。

## 变更历史

- 2026-06-29：创建。M4（数据结构深化扩充）全收官后，Owner 提出补充算法侧分类，先把规划落成文档防止以后重想。盘点确定 7 个算法大类、分三阶段（一：图/DP/回溯；二：字符串/查找；三：数学/思想），各列代表算法 + 共用可视化基础 + 首发项。承接 backlog.md 早先剥离的「图算法独立轨道」。对应 roadmap M6（产品内容线，与 M5 增长线并行）。**待 Owner 拍板启动阶段一首项（建议 G1 Dijkstra 最短路，承接图结构最顺）**。
- 2026-06-29：**G1 Dijkstra 最短路出池开工**（C-037，图算法大类开张）。Owner「继续」拍板从图算法切入。新增「图算法」顶层菜单分类（第 3 个，与数据结构/排序并列）+ Dijkstra 单源最短路页（固定 6 点 9 边带权有向图、单步松弛演示）。**实操调整**：原计划「扩展现有 GraphViz」改为**新建自包含 useDijkstra/DijkstraViz**（复用 GraphViz 六边形布局 + SVG 画法思路，但不改 C-022 已 verified 的 useGraph/GraphViz，零回归更稳）；图画布的共用抽取推迟到图算法第二项（如 Bellman-Ford）。编号顺延 037。
- 2026-06-29：**G1 完成 verified**（C-037）。22 Case（useDijkstra 12 + DijkstraViz 8 + 视图 2 + e2e 1）+ 改 TC-HOOK-01-1 两处（顶层分类 2→3）；单测 792 + e2e 31 全绿、覆盖率 93.11%，双轨部署。确定序 A→C→B→D→E→F、距离 [0,3,1,4,7,9]、最短路树点亮。**图算法大类正式开张**（第 3 个顶层分类）。下一项可选 G3 Bellman-Ford（负权，届时抽共用带权图画布）/ G5 拓扑 / G6 Kruskal（呼应并查集）；或转 DP/回溯首发。
- 2026-06-30：**G6 Kruskal 最小生成树出池开工**（C-038，图算法第 2 项）。Owner「继续图算法」中选定。图算法分类追加第 2 个算法（不新建分类）+ Kruskal 页（固定 6 点 9 边无向带权图、并查集判环、单步演示，MST 5 条总权 18、跳过 4 条成环）。复用 C-037 Algorithm 目录 + 单步 steps 范式、自包含（仍未抽共用带权图画布，留待 Bellman-Ford 等同布局算法时）；呼应 C-029 并查集。编号顺延 038（日期 6/30）。
- 2026-06-30：**G6 完成 verified**（C-038）。22 Case（useKruskal 12 + KruskalViz 8 + 视图 2 + e2e 1）+ 改 TC-HOOK-01-1 两处（图算法 children 1→2）；单测 814 + e2e 32 全绿、覆盖率 93.26%，双轨部署。MST=AC,BC,DE,BD,DF 总权 18、跳过 4 条成环、边排序列表 + 图同步高亮。**图算法分类已含 2 个算法**（最短路 Dijkstra + 最小生成树 Kruskal）。下一项可选：续图算法（G3 Bellman-Ford 负权→届时抽共用带权图画布 / G5 拓扑 / G7 Prim 对照 Kruskal / G8 SCC）；或转 M7 排序（S1 基数）/ DP / 回溯首发。
